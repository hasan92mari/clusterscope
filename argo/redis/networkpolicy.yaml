apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: clusterscope-redis-cnp
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  endpointSelector:
    matchLabels:
      app: clusterscope-redis
  ingress:
    - fromEndpoints:
        - matchLabels:
            "k8s:io.kubernetes.pod.namespace": clusterscope-frontend
            "k8s:app": clusterscope-frontend
      toPorts:
        - ports:
            - protocol: TCP
              port: "6379"
  egress:
    - toEndpoints:
        - matchLabels:
            "k8s:io.kubernetes.pod.namespace": kube-system
            "k8s:k8s-app": kube-dns
      toPorts:
        - ports:
            - protocol: UDP
              port: "53"
            - protocol: TCP
              port: "53"
