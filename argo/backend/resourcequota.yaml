apiVersion: v1
kind: ResourceQuota
metadata:
  name: clusterscope-backend-quota
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  hard:
    pods: "10"
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1"
    limits.memory: "1Gi"
