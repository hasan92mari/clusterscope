apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: clusterscope-pool
  namespace: metallb-system
spec:
  addresses:
    - 192.168.64.240/32
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: clusterscope-advertisement
  namespace: metallb-system
spec:
  ipAddressPools:
    - clusterscope-pool