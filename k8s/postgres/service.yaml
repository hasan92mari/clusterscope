apiVersion: v1
kind: Service
metadata:
  name: clusterscope-postgres
  namespace: clusterscope
  labels:
    app: clusterscope-postgres

spec:
  type: ClusterIP

  selector:
    app: clusterscope-postgres

  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP